# postal

An opinionated and direct HTTP POST request utility configurable from the command line.

## Installation

```bash
git clone "https://github.com/oh-clever/postal"
cd postal
cargo install --path .
```
